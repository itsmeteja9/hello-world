{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "Express demo deployed to Cloud Run through GitHub Actions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-include=app.js --test-coverage-lines=90"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
