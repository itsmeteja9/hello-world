apiVersion: v1
kind: Service
metadata:
  name: hello-world
  namespace: harness-delegate-ng
spec:
  type: LoadBalancer
  selector:
    app: hello-world
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
